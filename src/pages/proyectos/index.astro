---
import { getCollection } from 'astro:content';
import Layout from '../../layouts/Layout.astro';

// Obtener todos los proyectos y ordenar por order desc
const allProjects = await getCollection('projects');
const projects = allProjects.sort((a, b) => (b.data.order || 0) - (a.data.order || 0));
---

<Layout 
  title="Proyectos | Jonathan Guarirapa - Desarrollador Web"
  description="Explora mi portfolio de proyectos de desarrollo web. E-commerce, aplicaciones web, landing pages y más. Proyectos realizados con las últimas tecnologías."
>
  <!-- Hero Section -->
  <section class="relative min-h-[60vh] flex items-center justify-center overflow-hidden">
    <div class="absolute inset-0 bg-gradient-to-br from-nextsite-bg-darker via-nextsite-dark-blue to-nextsite-bg-dark"></div>
    
    <div class="relative z-10 max-w-6xl mx-auto px-4 text-center">
      <div class="animate-fade-in-up">
        <h1 class="text-5xl md:text-7xl font-bold mb-6">
          <span class="text-white">Mis</span>
          <br>
          <span class="bg-gradient-to-r from-cyan-400 to-blue-500 bg-clip-text text-transparent">
            Proyectos
          </span>
        </h1>
        <p class="text-xl md:text-2xl text-gray-300 mb-8 max-w-3xl mx-auto leading-relaxed">
          Una colección de mis trabajos más recientes que demuestran mi experiencia y creatividad
        </p>
      </div>
    </div>
  </section>

  <!-- Projects Grid -->
  <section class="py-20 bg-nextsite-bg-dark">
    <div class="max-w-7xl mx-auto px-4">
      {projects.length > 0 ? (
        <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
          {projects.map((project) => (
            <article class="card reveal" data-tilt>
              <!-- Cover Image -->
              {project.data.cover && (
                <div class="h-48 bg-gradient-to-br from-cyan-400 to-blue-500 flex items-center justify-center relative overflow-hidden mb-6 rounded-t-2xl">
                  <img 
                    src={project.data.cover} 
                    alt={project.data.title}
                    class="w-full h-full object-cover"
                    onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';"
                  />
                  <div class="absolute inset-0 bg-black/20 flex items-center justify-center" style="display: none;">
                    <span class="text-6xl">💻</span>
                  </div>
                  {project.data.featured && (
                    <div class="absolute top-4 right-4">
                      <span class="badge">Destacado</span>
                    </div>
                  )}
                </div>
              )}
              
              <div class="p-6">
                <header class="mb-4">
                  <h3 class="text-2xl font-bold text-white mb-2">{project.data.title}</h3>
                  <p class="text-gray-400 leading-relaxed">{project.data.summary}</p>
                </header>
                
                <!-- Stack -->
                {project.data.stack && project.data.stack.length > 0 && (
                  <div class="mb-6">
                    <div class="flex flex-wrap gap-2">
                      {project.data.stack.map(tech => (
                        <span class="pill">{tech}</span>
                      ))}
                    </div>
                  </div>
                )}
                
                <!-- Actions -->
                <footer class="flex gap-3">
                  <a href={`/proyectos/${project.slug}`} class="btn flex-1 text-center">
                    Ver Detalle
                  </a>
                  {project.data.repoUrl && (
                    <a 
                      href={project.data.repoUrl} 
                      target="_blank" 
                      rel="noopener noreferrer"
                      class="btn"
                      style="background: transparent; color: var(--text); outline: 1px solid rgba(255,255,255,.15); min-width: auto; padding: .9rem 1rem;"
                    >
                      <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
                        <path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/>
                      </svg>
                    </a>
                  )}
                </footer>
              </div>
            </article>
          ))}
        </div>
      ) : (
        <div class="text-center py-20">
          <div class="w-32 h-32 mx-auto mb-8 rounded-full bg-gradient-to-r from-cyan-400 to-blue-500 flex items-center justify-center">
            <span class="text-6xl">📁</span>
          </div>
          <h3 class="text-2xl font-bold text-white mb-4">No hay proyectos disponibles</h3>
          <p class="text-gray-400 mb-8">Los proyectos se están cargando o no hay contenido disponible.</p>
          <a href="/contacto" class="btn">
            Trabajar Conmigo
          </a>
        </div>
      )}
    </div>
  </section>

  <!-- CTA Section -->
  <section class="py-20">
    <div class="max-w-4xl mx-auto px-4 text-center">
      <div class="animate-fade-in-up">
        <h2 class="text-4xl md:text-5xl font-bold text-white mb-6">
          ¿Te gusta lo que <span class="text-nextsite-accent-blue">ves?</span>
        </h2>
        <p class="text-xl text-gray-400 mb-12 max-w-3xl mx-auto leading-relaxed">
          Trabajemos juntos para crear algo increíble para tu proyecto
        </p>
        
        <div class="flex flex-col sm:flex-row gap-6 justify-center">
          <a href="/contacto" class="btn animate-pulse-glow">
            <span class="flex items-center gap-2">
              Iniciar Proyecto
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
              </svg>
            </span>
          </a>
          <a href="/servicios" class="btn" style="background: transparent; color: var(--text); outline: 1px solid rgba(255,255,255,.15);">
            <span class="flex items-center gap-2">
              Ver Mis Servicios
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
              </svg>
            </span>
          </a>
        </div>
      </div>
    </div>
  </section>
</Layout>

<script>
  import { enableButtonRipple, revealOnScroll, applyStagger } from '../../scripts/motion.js';
  import { enableTilt } from '../../scripts/tilt.js';
  enableButtonRipple(); revealOnScroll(); applyStagger(); enableTilt();
</script>
